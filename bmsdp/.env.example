# Copy to .env (gitignored) and adjust for your machine.
# VS Code's Python extension loads this automatically for both the
# integrated terminal's Python processes and the Test Explorer.
JAVA_HOME=/opt/homebrew/opt/openjdk@17
